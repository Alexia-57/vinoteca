databases:
  - name: Vinoteca
    databaseName: Vinoteca
    user: Vinoteca

services:
  - type: web
    name: mysite
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: Vinoteca
          property: connectionString
      - key: 118b443047a9bda7a05056c3502b5e0f
        sync: false
